version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: modernjs-nginx
    ports:
      - "8080:80"
    volumes:
      - ../packages/app/dist:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: always
