import { en } from './en.js';
import { fr } from './fr.js';

export const LANGUAGES = {
    en: {
        code: 'en',
        label: 'EN',
        translations: en
    },
    fr: {
        code: 'fr',
        label: 'FR',
        translations: fr
    }
};

export const DEFAULT_LANGUAGE = 'en';
